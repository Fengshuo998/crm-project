<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myCode.dao.EmployeeMapper">
    <select id="selectOneById" resultType="Employee">
        select emp_id,emp_name,emp_email,sex,department_id from tbl_emp
            where emp_id=#{empId}
    </select>
    <select id="selectOneByName" resultType="Employee">

    </select>
    <select id="selectWithDeptById" resultType="Employee">

    </select>
    <select id="selectByLimitAndOffset" resultType="Employee">

    </select>
    <delete id="deleteOneById">
        delete from tbl_emp where id = #{empId}
    </delete>
    <update id="updateOneById">

    </update>
    <insert id="insertOne">

    </insert>
</mapper>